export enum BlockLevel {
  low = 1, // Text、Image
  medium = 5, // Box
  heigh = 10, // Section
  highest = 100 // Page
}

export enum BlockType {
  PAGE = 'page',
  SECTION = 'section',
  BOX = 'box',
  IMAGE = 'image',
  TEXT = 'text',
  PARAGRAPH = 'paragraph'
}

